# agent-team

> Status: **placeholder** — implementation pending.

Multi-agent team coordination for long-running collaborative Claude Code sessions. The intent is to package the lead/teammate workflow (spawn long-lived workers, route messages via `SendMessage`, share an inbox/task list) as a reusable plugin.

## Install

```
/plugin install agent-team@fomalhaut647-plugins
```
